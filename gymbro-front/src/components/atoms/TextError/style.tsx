import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
    container: {
        display:'flex',
    },
    textError:{
        fontSize:'20px',
        color:'#F94C66',
        marginLeft:'11px'
    }
}));